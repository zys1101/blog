{
  "name": "devlog-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "hash-password": "ts-node -e \"import { createInterface, Interface } from 'node:readline'; import { hash } from 'bcrypt'; const input: Interface = createInterface({ input: process.stdin, output: process.stdout }); input.question('Password (visible; local terminal only): ', (password: string): void => { input.close(); if (password.length < 12 || Buffer.byteLength(password, 'utf8') > 72) { console.error('Use 12+ characters, at most 72 UTF-8 bytes'); process.exitCode = 1; return; } void hash(password, 12).then((value: string): void => { console.log(value); }); });\""
  },
  "dependencies": {
    "@nestjs/common": "^11.1.0",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.1.0",
    "@nestjs/jwt": "^11.0.0",
    "@nestjs/passport": "^11.0.5",
    "@nestjs/platform-express": "^11.1.0",
    "@nestjs/typeorm": "^11.0.0",
    "bcrypt": "^6.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "mysql2": "^3.14.1",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "typeorm": "^0.3.24"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.7",
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.0",
    "@types/passport-jwt": "^4.0.1",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  }
}
